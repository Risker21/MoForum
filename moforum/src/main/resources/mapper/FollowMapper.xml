<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moforum.mapper.FollowMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_follow (follower_id, followed_id) VALUES (#{followerId}, #{followedId})
    </insert>
    <delete id="delete">
        DELETE FROM t_follow WHERE follower_id = #{followerId} AND followed_id = #{followedId}
    </delete>
    <select id="selectByFollowerAndFollowed" resultType="com.moforum.entity.Follow">
        SELECT * FROM t_follow WHERE follower_id = #{followerId} AND followed_id = #{followedId}
    </select>
    <select id="selectFollowers" resultType="com.moforum.entity.Follow">
        SELECT * FROM t_follow WHERE followed_id = #{userId} ORDER BY create_time DESC
    </select>
    <select id="selectFollowing" resultType="com.moforum.entity.Follow">
        SELECT * FROM t_follow WHERE follower_id = #{userId} ORDER BY create_time DESC
    </select>
    <select id="countFollowers" resultType="int">
        SELECT COUNT(*) FROM t_follow WHERE followed_id = #{userId}
    </select>
    <select id="countFollowing" resultType="int">
        SELECT COUNT(*) FROM t_follow WHERE follower_id = #{userId}
    </select>
</mapper>
