<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moforum.mapper.UserMapper">
    <select id="selectById" resultType="com.moforum.entity.User">
        SELECT id, username, password, user_no, bio, avatar_url, create_time, update_time FROM t_user WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, create_time, update_time)
        VALUES (#{username}, #{password}, NOW(), NOW())
    </insert>

    <select id="selectByUsername" resultType="com.moforum.entity.User">
        SELECT * FROM t_user WHERE username = #{username}
    </select>

    <select id="selectByUserNo" resultType="com.moforum.entity.User">
        SELECT * FROM t_user WHERE user_no = #{userNo}
    </select>

    <update id="updateUserNo">
        UPDATE t_user SET user_no = #{userNo}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateProfile">
        UPDATE t_user
        SET bio = #{bio}, avatar_url = #{avatarUrl}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE t_user SET password = #{password}, update_time = NOW() WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

    <select id="searchByKeyword" resultType="com.moforum.entity.User">
        SELECT id, username, user_no AS userNo, create_time AS createTime
        FROM t_user WHERE username LIKE #{keyword}
        ORDER BY create_time DESC LIMIT #{limit}
    </select>
</mapper>